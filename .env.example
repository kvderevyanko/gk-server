# Copy values to your shell, deployment environment, or an ignored .env file.
# Do not commit the real value.
COOKIE_VALIDATION_KEY=replace-with-a-random-32-byte-secret
ESP_HTTP_TOKEN=
# Keep the default loopback binding when a host reverse proxy handles HTTP.
HTTP_BIND_ADDRESS=127.0.0.1
HTTP_PORT=8080
